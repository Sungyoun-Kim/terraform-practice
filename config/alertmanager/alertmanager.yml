global:
  resolve_timeout: 5m

route:
  receiver: local-learning
  group_by:
    - alertname
    - job
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 2h

receivers:
  - name: local-learning
