services:
  registry:
    image: registry:2
    container_name: terraform-practice-registry
    ports:
      - "5001:5000"
    volumes:
      - registry-data:/var/lib/registry
    restart: unless-stopped

volumes:
  registry-data:
